---
title: "Home"
output: html_document
---

```{r setup, include=FALSE}
library(htmltools)
knitr::opts_chunk$set(echo = FALSE)
```

```{r landing}
# Links for tiles and CTAs
menu_items <- list(
  list(href = "about.html",       label = "Trainers",   icon = "👥"),
  list(href = "eda.html",         label = "EDA",        icon = "📊"),
  list(href = "analysis.html",    label = "Analysis",   icon = "🧠"),
  list(href = "final_report.html",label = "Report",     icon = "📜")
)

tiles <- lapply(menu_items, function(x) {
  tags$a(
    class = "home-tile",
    href  = x$href,
    `aria-label` = x$label,
    tags$span(class = "home-tile__icon", x$icon),
    tags$span(class = "home-tile__label", x$label)
  )
})

hero <- tags$section(
  class = "home-hero",
  tags$div(class = "home-hero__bg"),
  tags$div(
    class = "home-hero__content",
    tags$div(class = "home-hero__pokeball", `aria-hidden` = "true"),
    tags$h1(class = "home-hero__title", "Gotta Graph ’Em All"),
    tags$p(class = "home-hero__subtitle",
           "Data science adventures in the Pokémon universe"),
    tags$div(
      class = "home-hero__ctas",
      tags$a(class = "btn-cta btn-cta--primary", href = "eda.html",  "Explore EDA"),
      tags$a(class = "btn-cta btn-cta--primary",   href = "about.html","Meet the Trainers"),
      tags$a(class = "btn-cta btn-cta--primary", href = "analysis.html","Explore Analytics")
    )
  )
)

grid <- tags$section(
  class = "home-grid",
  tags$h2(class = "home-grid__title", "Choose your next quest"),
  tags$div(class = "home-grid__tiles", tiles)
)

features <- tags$section(
  class = "home-features",
  tags$div(
    class = "home-feature",
    tags$div(class = "home-feature__icon", "🎯"),
    tags$h3("Interactive EDA"),
    tags$p("Zoom, filter, and explore Pokémon stats by type, generation, and more.")
  ),
  tags$div(
    class = "home-feature",
    tags$div(class = "home-feature__icon", "🧬"),
    tags$h3("Statistical Insights"),
    tags$p("Explore whether types are stronger than others, if legendaries truly are stronger, and if we can predict legendary status.")
  ),
)

# Return the composed page (htmltools renders this properly)
tagList(hero, grid, features)
```